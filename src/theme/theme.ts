"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9775fa",
    },
    secondary: {
      main: "#f06595",
    },
  },
});

export default theme;
